[package]
name = "solverforge"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true
description = "SolverForge - A constraint solver in Rust"

[features]
default = []
decimal = ["solverforge-core/decimal"]
serde = ["solverforge-core/serde"]
console = ["dep:solverforge-console"]
verbose-logging = ["solverforge-console/verbose-logging"]

[dependencies]
solverforge-core = { version = "0.19.3", path = "../solverforge-core" }
solverforge-bridge = { version = "0.19.3", path = "../solverforge-bridge" }
solverforge-macros = { version = "0.19.3", path = "../solverforge-macros" }
solverforge-scoring = { version = "0.19.3", path = "../solverforge-scoring" }
solverforge-solver = { version = "0.19.3", path = "../solverforge-solver" }
solverforge-config = { version = "0.19.3", path = "../solverforge-config" }

solverforge-cvrp = { version = "0.19.3", path = "../solverforge-cvrp" }

# Console output (optional)
solverforge-console = { version = "0.19.3", path = "../solverforge-console", optional = true }
